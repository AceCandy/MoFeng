# 全部前端页面逐页精修：实施计划

## 1. 建立确定性页面验收

- [x] 盘点九页、详情 8 分区、设置 4 分区、管理 6 分区及兼容重定向。
- [x] 复用并按需最小扩展 E2E fixture，使普通用户、管理员、注册开放/关闭和主要页面数据可确定性访问。
- [x] 用 agent-browser 在 `1440×900` 与 `390×844` 完成一次成批初审，逐页记录当前可复现问题、console、axe 与横向溢出。
- 验证：每个页面有状态/视口/问题结论；历史报告建议必须重新复现后才能进入修复。

## 2. 认证、工作台与灵感精修

- [x] 压缩登录/注册移动引子，校准表单首屏位置和主次动作。
- [x] 注册实现字段级错误、帮助文本、焦点与 ARIA 关联；登录失败补真实恢复说明。
- [x] 工作台收紧移动 hero，统一状态驱动主动作和项目下一步。
- [x] 灵感恢复显示项目/阶段，移动端避免强制聚焦，退出提示说明持久化边界。
- 验证：相关 Vitest/静态合同；浏览器覆盖默认、失败、空态/有数据与移动首屏。

## 3. 项目详情与写作台精修

- [x] 长梗概按原始换行分段，覆盖空文本与长文本。
- [x] 章节目录增加号/标题搜索、无结果与清除路径，复用现有选择和滚动。
- [x] 助手未定稿信息改为低负担 disclosure，默认仅一个信息组展开；完成章梳理保持直接展示。
- [x] 复验 8 个详情分区、章节抽屉、助手抽屉、100 章长目录和 833/834/1199/1200 边界。
- 验证：最小组件测试、写作台现有 E2E、桌面/移动键盘与触控检查。

## 4. 设置与管理精修

- [x] 复验并修复设置 4 分区的 URL、ARIA、模型弹层、移动密度和状态问题。
- [x] Prompt 增加 name/title/tag 搜索；移动端实现列表→编辑/返回且不丢未保存内容。
- [x] 复验管理 6 分区的加载、空态、危险操作与长内容；修正管理员项目完成状态语义和内部滚动焦点。
- 验证：相关 Vitest、路由前进/后退、脏状态取消/保存、管理员 axe 与移动交互。

## 5. 集成复核与提交

- [x] 运行 Impeccable detector，一次集中修复全部真实发现，最多一次浏览器复验。
- [x] 运行 `npm run lint`、`npm run type-check`、全量 `npm run test:unit` 和相关 `npm run test:e2e`。
- [x] 独立审查完整 diff，检查设计合同、范围、孤儿代码、硬编码颜色、临时文件与隐私产物。
- [x] 逐页覆盖清单全部落为“通过”或带原因的“未验证”，不能以全局样式替代页面证据。
- [x] 关闭全部调试服务和浏览器会话，`git diff --check` 与工作树分类通过后提交。

## Rollback Points

- 认证页、创作页、设置/管理页保持独立文件批次；某批回归时只回退所属批次。
- fixture/test 改动只用于确定性验收，不改变生产构建；如 fixture 扩展失效可单独回退，不以删除产品回归测试替代修复。

## 最终逐页验收记录

| 页面 | 桌面与移动证据 | 结果 |
| --- | --- | --- |
| `/login` | `1440×900`、`390×844`；默认、失败恢复说明、术语、口令按钮触控与 axe | 通过 |
| `/register` | 双视口；首屏表单、空提交首错聚焦、四字段独立 invalid、验证码同行布局 | 通过 |
| `/workspace` | 双视口；有项目、状态驱动主动作、移动 hero 与项目库首屏位置 | 通过 |
| `/inspiration` | 双视口；恢复项目/阶段、移动端不自动聚焦、退出持久化说明 | 通过 |
| `/projects/:id` | 双视口与详情 8 分区；长梗概三段渲染、完成状态语义 | 通过 |
| `/projects/:id/write` | 双视口及 833/834、1199/1200；100 章搜索/跳转、双抽屉、disclosure、axe | 通过 |
| `/settings` | 双视口与 4 分区；query 直达/刷新、脏状态确认、弹层与 ARIA 关联 | 通过 |
| `/admin` | 双视口与 6 分区；Prompt 搜索、移动列表→编辑/返回、草稿保留、axe | 通过 |
| `/admin/novels/:id` | 双视口与详情 8 分区；只读完成状态、内部滚动与长内容 | 通过 |
| 兼容重定向 | `/detail/:id`、`/novel/:id`、`/admin/novel/:id` | 目标正确 |

构造范围外未执行真实邮件发送、真实注册落库、生产第三方模型调用和破坏性管理操作；本任务使用确定性 fixture 验证对应页面状态与交互，不触碰后端、生产数据或鉴权规则。

最终质量证据：ESLint、`vue-tsc`、44 个 Vitest 文件 360 项、Playwright 26 项、生产构建与体积预算、Impeccable detector、`git diff --check` 全部通过；调试服务、浏览器会话和临时产物已清理。
