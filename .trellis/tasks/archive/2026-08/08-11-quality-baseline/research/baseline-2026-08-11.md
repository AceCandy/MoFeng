# 2026-08-11 质量基线

## 执行环境

- 后端使用 `backend/.venv/bin/python` 及该虚拟环境内的 pytest、Ruff、Black、mypy。
- 前端使用现有 `frontend/node_modules` 和 Playwright Chromium。
- Playwright 维持 `workers=1`、`retries=0`，执行 desktop/mobile 两个项目。
- 后端、前端静态门和 Playwright 同时采集结果；因此 eslint 扫描
  `frontend/test-results` 时与 Playwright 写入/删除目录发生竞态，该次 lint 结果无效，
  需在浏览器测试停止并清理产物后单独复测。

## 后端

| 命令 | 状态 | 实测结果 | 初步归因 |
| --- | --- | --- | --- |
| `.venv/bin/python -m pytest -q` | 失败 | 698 通过，9 失败，11 warnings | 3 个静态断言漂移/执行时工作树变化；4 个日志断言受全局 logging 配置顺序污染；1 个版本数断言依赖本机配置；1 个独立进程测试耗尽 PostgreSQL 连接 |
| `.venv/bin/python -m ruff check app tests` | 失败 | 166 项，其中 126 项可自动修复 | 91 个 I001、33 个 F401；其余为 E402/F821/F841 及少量真实语义问题，必须分批处理 |
| `.venv/bin/python -m black --check app tests` | 失败 | 133 个文件需格式化，119 个文件不变 | 历史格式漂移；只对 `app tests` 做独立机械批次 |
| `.venv/bin/python -m mypy` | 通过 | 20 个限定源文件无问题 | 仅代表 `pyproject.toml` 中 durable workflow/job 范围 |
| `.venv/bin/python -m compileall -q app` | 通过 | 退出 0 | 无语法编译错误 |

### pytest 失败明细

- `test_admin_overview_shell_has_shared_global_styles`：失败输出未看到
  `.admin-ops__metric`；当前工作树的聚合样式已包含该 selector，需定向复测确认。
- `test_workspace_reads_query_loading_error_and_refetch_state`：失败输出未看到
  `useImportNovelMutation`；当前工作树已包含该调用，需定向复测确认。
- `test_impeccable_design_sidecar_matches_current_warm_paper_tokens`：测试仍断言旧蓝灰 token，
  当前 sidecar 已切换到暖纸/朱砂设计，属于测试漂移。
- 章节工作流 activity 与 optimizer 的 4 个日志断言：`app.main` 的 `dictConfig` 会让
  `app.*` 日志不再传播到 root；pytest `caplog` 依赖导入顺序，属于测试隔离问题，生产日志
  已执行且没有记录异常消息。
- `test_production_handler_runs_selection_finalize_and_projection_resume`：测试未冻结
  `versions`，实际读取环境中的 `writer_chapter_versions=2` 后生成 2 个版本；测试期望 1，
  属于配置耦合。
- `test_production_workflow_recovers_across_process_kills_redis_off_and_rollout`：子进程在
  `register_worker` 时收到 PostgreSQL `TooManyConnectionsError`；测试 worker 使用默认连接池，
  并发基线又同时占用连接，属于测试资源上限问题。

## 前端静态门

| 命令 | 状态 | 实测结果 | 初步归因 |
| --- | --- | --- | --- |
| `npm run api:check` | 失败 | OpenAPI 检查启动前即 `ModuleNotFoundError: fastapi` | npm 脚本固定调用系统 `python3`，绕过已有 `backend/.venv` |
| `npm run lint` | 无效 | eslint 退出 2，`ENOENT scandir frontend/test-results` | 与同时运行的 Playwright 产物目录发生竞态，清理后串行复测 |
| `npm run type-check` | 通过 | 退出 0 | `vue-tsc --build` 通过 |
| `npm run test:unit` | 通过 | 31 个文件、291 个测试通过 | 无失败 |
| `npm run build` | 通过 | Vite 构建和 bundle budget 均通过 | JS 总 gzip 569.45 KB、CSS 总 gzip 81.47 KB；存在非阻断预警 |

Bundle budget 的非阻断预警：JS 总量超过 560 KB warning 线；
`assets/index-*.css` 25.14 KB 超过 24 KB 单文件 warning 线，但都低于硬上限。

## Playwright

| 命令 | 状态 | 实测结果 | 初步归因 |
| --- | --- | --- | --- |
| `npm run test:e2e` | 失败 | 2 通过，18 失败，耗时 2.9 分钟 | `WDWorkspace` 在 running/submitting/finalizing/projectionPending/succeeded 阶段卸载 `ChapterWorkflowPanel`，现有 role/live-region/heading 契约随之消失 |

Playwright locator 会在每次断言时重新解析，不存在“初始化 locator 过期”的问题。
`ChapterWorkflowPanel` 自身已经覆盖所有阶段文案和 `status|alert` 语义，最小修复应让页面在这些
阶段继续装配面板，而不是放宽测试断言。

本轮 Playwright 管理的 6173/6181 端口已停止监听；`test-results` 和
`playwright-report` 已清理。

## 最终复测

### 后端最终门

| 命令 | 状态 | 最终结果 |
| --- | --- | --- |
| `.venv/bin/python -m pytest -q` | 通过 | 707 通过，11 warnings，耗时 203.24 秒 |
| `.venv/bin/python -m ruff check app tests` | 通过 | All checks passed |
| `.venv/bin/python -m black --check app tests` | 通过 | 252 个文件无需修改 |
| `.venv/bin/python -m mypy` | 通过 | 当前 durable workflow/job 限定范围内 20 个源文件无问题 |
| `.venv/bin/python -m compileall -q app` | 通过 | 退出 0 |

pytest warnings 均为既有 Pydantic v1 风格 validator/class Config 和 passlib `crypt`
弃用提示，不影响退出状态。本轮未扩大 mypy 的 `files` 范围，也未修改
`backend/alembic/versions/` 下的历史 revision。

共享 PostgreSQL 的 `max_connections=100`。调查期间其他应用连接峰值达到 92；目标迁移测试
自身峰值为 5，套件采样峰值约为 4。初始 `TooManyConnections` 属共享服务瞬时容量竞争，
不是生产连接泄漏；后续两次后端全量 pytest 均为 707/707，因此未修改生产连接池配置。

### 前端最终门

| 命令 | 状态 | 最终结果 |
| --- | --- | --- |
| `npm run api:check` | 通过 | OpenAPI byte check、TypeScript 生成 check、2 个 ownership 测试和所有权扫描全部通过 |
| `npm run lint` | 通过 | eslint 退出 0 |
| `npm run type-check` | 通过 | vue-tsc 退出 0 |
| `npm run test:unit` | 通过 | 31 个文件、291 个测试通过 |
| `npm run build` | 通过 | 3238 个模块构建完成，bundle budget 退出 0 |

最终构建的 JS 总 gzip 为 569.50 KB，CSS 总 gzip 为 81.47 KB；JS 总量和
`assets/index-*.css` 仍触发既有非阻断 warning，但均低于硬上限。

### Playwright 最终门

| 命令 | 状态 | 最终结果 |
| --- | --- | --- |
| 外部重试定向 desktop/mobile | 通过 | 2/2，验证真实生成控制台入口、风险确认和命令 payload |
| `npm run test:e2e` | 通过 | desktop/mobile Chromium 20/20，耗时 36.9 秒 |

Playwright 配置保持 `workers=1`、`retries=0`。最终运行后已删除 `test-results`、
`playwright-report`，未生成 `blob-report`；6173/6181 均无监听。diff 复核未发现新增
skip、xfail、永久 ignore、`continue-on-error`、retry 或关键断言弱化。
