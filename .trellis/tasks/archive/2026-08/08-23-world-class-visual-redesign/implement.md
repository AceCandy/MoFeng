# 实施计划

## 1. 建立新世界基础

- [x] 在 `index.html` 写入可进入生产构建的方向合同。
- [x] 重写全局色彩、字体、形态、焦点和动效令牌。
- [x] 增加共享 `world-class.css` 迁移层并接入 `main.css`；认证专属规则按路由拆入 `world-class-auth.css` 以满足 bundle 硬预算。
- [x] 对齐 `App.vue` 的 Naive UI 主题覆盖。
- [x] 验证：生产构建后可检索概念种子 `94f236b2`，无暗色主题分支。

## 2. 重做共享壳与入口

- [x] 重做 `AppShell` 的导航、项目上下文和内容框架，不改业务事件。
- [x] 重做 `AuthLayout`、登录和注册的专属构图。
- [x] 验证：登录/注册桌面与移动可操作；登录、OAuth、注册状态测试契约仍在。

## 3. 重做核心创作页面

- [x] 工作区改为严格分舞台首屏与连续项目档案。
- [x] 灵感页突出对话主舞台并收敛辅助栏。
- [x] 详情共享壳、章节区与写作台统一为 cue-book 工作面。
- [x] 验证：项目跳转、章节选择、生成/版本/朗读状态及三个响应式区间不退化。

## 4. 重做配置与治理页面

- [x] 设置页形成能力链与明确配置工作面。
- [x] 管理后台形成系统控制台与任务域导航。
- [x] 管理员详情明确只读身份。
- [x] 验证：query tab、键盘切换、未保存确认、强制改密契约不退化。

## 5. 集中浏览器复验

- [x] 启动 fixture 与 Vite；使用 1440×900 和 Pixel 7 批量截图九页关键状态。
- [x] 第一轮集中记录视觉、溢出、焦点和错误状态问题并一次修复。
- [x] 第二轮只做确认，不继续开放式微调。
- [x] 调试服务在结束前关闭。

## 6. 独立质量门

- [x] 对所有改动目标运行一次 Impeccable detector，修复机械问题，不重复运行。
- [x] 独立复核功能 diff、视觉合同、响应式和无障碍。
- [x] 运行 `vue-tsc`、Vitest、生产构建及相关 Playwright 两种设备项目。
- [x] 按实际实现重写并复核 `DESIGN.md` 与 `.impeccable/design.json`。
- [x] 提交工作、归档任务并记录 journal。

## 回滚点

- 共享基础、页面实现、检测修复都保留在同一任务 diff 中；若视觉合同失败，整次工作提交可一次回滚，不拆出依赖后端的中间状态。
